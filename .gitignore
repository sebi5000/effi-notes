# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Build output
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo

# Test / coverage
coverage/
.nyc_output/
*.lcov

# Env files
.env
.env.*
!.env.example

# IDE / OS
.DS_Store
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun-debug.log*

# Bun – lockfile IS committed for reproducibility

# Prisma generated client
packages/db/generated/

# Local data / backups
.local/
backups/
# smoke.sh writes timestamped snapshots into the repo root for the
# backup-roundtrip assertion, then deletes them — but a crashed run
# leaves them behind.
.smoke-backup-*/

# Lefthook local
.lefthook-local.yml

# Superpowers brainstorm companion (local mockups)
.superpowers/

# Claude Code – local user settings (project agents/CLAUDE.md ARE committed)
.claude/settings.local.json
